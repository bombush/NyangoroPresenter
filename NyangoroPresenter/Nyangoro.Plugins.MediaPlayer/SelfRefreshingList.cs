using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

//unused unfinished class. To be scrapped later?
/*
namespace Nyangoro.Plugins.MediaPlayer
{
    class SelfRefreshingList<T>
    {
        List<T> originalItems;
        List<T> activeItems;
        List<T> disabledItems;

        public static SelfRefreshingList<T> CreateFromList(List<T> list)
        {
            SelfRefreshingList<T> newList = new SelfRefreshingList<T>();
            newList.originalItems = list;
            newList.activeItems = list;
            newList.disabledItems = new List<T>();
        }

        public void Add(T item)
        {

        }
    }
}*/
